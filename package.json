{
  "private": true,
  "scripts": {
    "format": "npx prettier . --write"
  },
  "workspaces": [
    "packages/core",
    "packages/parser"
  ],
  "devDependencies": {
    "prettier": "^3.3.2",
    "prettier-plugin-organize-imports": "^4.0.0"
  }
}
